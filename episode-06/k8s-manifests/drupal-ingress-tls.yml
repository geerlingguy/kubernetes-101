---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: drupal
  namespace: drupal
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod  # This helps our ClusterIssuer identify this Ingress resource.
spec:
  tls:  # This describes the TLS configuration for our site.
  - hosts:
    - ep6.kube101.filament-ds.com
    secretName: ep6-tls
  rules:
  - host: ep6.kube101.filament-ds.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: drupal
            port:
              number: 80

